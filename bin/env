#!/bin/bash

source bin/libs/inputs

manage.env.choose() {
    CHOICES=(
        "Development"
        "Production"
    )

    VALUES=(
        dev
        prod
    )

    inputs.choices UWKGM_ENV "Environment" "Choose environment" "dev" "$UWKGM_ENV" "${CHOICES[@]}" "${VALUES[@]}"
}