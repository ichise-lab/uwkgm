#!/bin/bash

formats.align.left() {
    if [[ $3 -gt ${#2} ]]; then
        SPACE_OUTPUT="$2"

        for SPACE_I in $(seq 0 $(($3 - ${#2})));
        do
            SPACE_OUTPUT+=" "
        done

        eval "$1=\"$SPACE_OUTPUT\""
    else
        eval "$1=\"$2\""
    fi
}