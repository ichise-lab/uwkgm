#!/bin/bash

function config() {
   KEY=${1::-1}
   VALUE=
   VALUES=($@)

   for i in $(seq 1 $((${#VALUES[@]} - 1))); do
      VALUE+="${VALUES[i]} "
   done

   if [[ ${!KEY} == "" ]]; then
      eval "export $KEY=$VALUE"
   fi
}
