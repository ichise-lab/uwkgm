from dorest.decorators import endpoint

@endpoint(['GET'])
def echo(text: str) -> str:
    """Echo a string

    :param text: A string to be echoed
           :ex: Test Echo
    
    :return: The exact same string
    """

    return text
