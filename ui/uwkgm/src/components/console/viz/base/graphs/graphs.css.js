import { makeStyles } from '@material-ui/core/styles';

export const styles = {
    graphs: makeStyles(theme => ({
        container: {
            
        },

        fixedContent: {
            paddingTop: 44,
            alignItems: 'center',
            justifyContent: 'center'
        }
    }))
}
